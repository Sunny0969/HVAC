const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.tsx') || file.endsWith('.ts')) results.push(file); } }); return results; } const files = walk('src/admin'); files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); let newC = c.replace(/to="\/admin\//g, 'to="/'); newC = newC.replace(/to=\{\\/admin\//g, 'to={\/'); newC = newC.replace(/navigate\('\/admin\//g, 'navigate(\'/'); newC = newC.replace(/navigate\(\\/admin\//g, 'navigate(\/'); if (c !== newC) { fs.writeFileSync(f, newC); console.log('Fixed', f); } });
