const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.tsx') || file.endsWith('.ts')) results.push(file); } }); return results; } const files = walk('src/admin'); files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); c = c.replace(/to="\/admin/g, 'to="'); c = c.replace(/to=\{\\/admin/g, 'to={\'); c = c.replace(/navigate\('\/admin/g, 'navigate(\''); c = c.replace(/Pick Pack Pro/gi, 'HVAC Exit Advisors'); c = c.replace(/PPP CMS/g, 'HVAC CMS'); c = c.replace(/ppp-leads-updated/g, 'hvac-leads-updated'); fs.writeFileSync(f, c); console.log('Fixed', f); });
